# 学术论文写作系统 - 项目结构文档

## 📋 项目概览

这是一个基于 **React + TypeScript + Vite** 的学术论文智能写作系统，集成了多种 AI 模型（DeepSeek、Qwen）来辅助学术论文的写作、讨论和修改。

### 核心功能
- 📝 **LaTeX 编辑器**：基于 Monaco Editor 的专业 LaTeX 编辑环境
- 🤖 **AI 写作助手**：支持讨论模式和撰写模式，可自动调用工具
- 📚 **资源管理**：管理参考文献、图片、PDF、数据文件、代码片段和笔记
- 🔍 **论文搜索**：集成 arXiv 论文搜索和下载功能
- 📄 **PDF 解析**：自动提取 PDF 文本内容用于分析

---

## 📁 目录结构

```
学术论文写作系统/
│
├── 📄 .gitignore            # Git 忽略配置
├── 📄 package.json          # 项目配置和依赖
├── 📄 vite.config.ts        # Vite 构建配置
├── 📄 tsconfig.json         # TypeScript 配置
├── 📄 tsconfig.node.json    # Node TypeScript 配置
├── 📄 index.html            # 入口 HTML
│
├── 📂 src/                  # 🎨 前端源码
│   ├── 📄 main.tsx              # React 入口
│   ├── 📄 App.tsx               # 主应用组件
│   ├── 📂 components/           # React 组件
│   │   ├── AgentChat.tsx            # AI 对话面板
│   │   ├── ApiKeyModal.tsx          # API Key 配置
│   │   ├── ErrorBoundary.tsx        # 错误边界
│   │   ├── MonacoEditor.tsx         # LaTeX 编辑器
│   │   ├── PreviewPanel.tsx         # 预览/工具面板
│   │   └── ResourcePanel.tsx        # 资源管理面板
│   ├── 📂 services/             # 服务层
│   │   ├── agentService.ts          # AI Agent 服务
│   │   └── resourceService.ts       # 资源管理服务
│   └── 📂 styles/               # 样式（模块化）
│       ├── index.css                # 样式入口
│       ├── base.css                 # 基础样式
│       ├── layout.css               # 布局样式
│       ├── responsive.css           # 响应式样式
│       └── 📂 components/           # 组件样式
│           ├── toolbar.css              # 工具栏
│           ├── chat.css                 # 聊天面板
│           ├── modal.css                # 弹窗
│           ├── form.css                 # 表单元素
│           └── tools.css                # 工具调用
│
├── 📂 server/               # 🖥️ 后端服务
│   ├── 📄 index.cjs             # 服务器入口
│   ├── 📄 config.cjs            # 配置常量
│   ├── 📄 storage.cjs           # 存储模块
│   ├── 📄 pdfUtils.cjs          # PDF 工具
│   ├── 📄 aiApi.cjs             # AI API
│   ├── 📂 tools/                # 工具模块 (拆分)
│   │   ├── index.cjs                # 统一导出
│   │   ├── toolDefinitions.cjs      # 工具定义
│   │   ├── toolExecutor.cjs         # 工具执行
│   │   └── toolParser.cjs           # 工具解析
│   └── 📂 routes/               # API 路由
│       ├── agent.cjs                # Agent 路由
│       ├── resources.cjs            # 资源路由
│       └── toolsRouter.cjs          # 工具路由
│
├── 📂 storage/              # 💾 数据存储
│   ├── references.json          # 参考文献
│   ├── images.json              # 图片
│   ├── pdfs.json                # PDF 文件
│   ├── datafiles.json           # 数据文件
│   ├── codesnippets.json        # 代码片段
│   └── notes.json               # 笔记
│
├── 📂 downloads/            # 📥 下载的论文 PDF
├── 📂 scripts/              # 🔧 工具脚本
│   └── test-pdf-storage.cjs     # PDF 存储测试
└── 📂 dist/                 # 📦 构建输出
```

---

## 🏗️ 架构设计

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端 (React + Vite)                       │
├─────────────────┬─────────────────┬─────────────────────────────┤
│   PreviewPanel  │  MonacoEditor   │        AgentChat            │
│   (左侧面板)     │   (中间编辑区)   │       (右侧对话)             │
│   - 工具栏       │   - LaTeX编辑   │   - AI对话                  │
│   - 资源管理     │   - 语法高亮    │   - 模式选择                 │
│   - API配置      │   - 自动完成    │   - 工具调用                 │
└────────┬────────┴────────┬────────┴────────────┬────────────────┘
         │                 │                      │
         └─────────────────┼──────────────────────┘
                           │ HTTP API
         ┌─────────────────▼──────────────────────┐
         │           后端 (Express.js)             │
         ├────────────────────────────────────────┤
         │  /api/agent      - AI 对话接口          │
         │  /api/tools      - 工具执行接口         │
         │  /api/resources  - 资源管理接口         │
         └────────────────┬───────────────────────┘
                          │
         ┌────────────────┼───────────────────────┐
         │                │                        │
    ┌────▼────┐     ┌─────▼─────┐           ┌─────▼─────┐
    │ DeepSeek│     │   Qwen    │           │   arXiv   │
    │   API   │     │   API     │           │   API     │
    └─────────┘     └───────────┘           └───────────┘
```

---

## 📦 前端组件详解

### 1. App.tsx - 主应用组件
**职责**：应用状态管理和布局控制

```typescript
// 核心状态
- editorContent: string     // 编辑器内容
- messages: Message[]       // 对话历史
- apiKey: string           // 当前 API Key
- apiKeyStatus: object     // API 配置状态

// 功能
- 自动保存（localStorage）
- API Key 状态检测
- 三栏布局管理
```

### 2. MonacoEditor.tsx - LaTeX 编辑器
**职责**：提供专业的 LaTeX 编辑环境

```typescript
// 功能特性
- 语法高亮 (LaTeX)
- 行号显示
- 迷你地图
- 自动换行
- 自动布局调整
```

### 3. AgentChat.tsx - AI 对话面板
**职责**：AI 对话交互和工具调用

```typescript
// 核心功能
- 讨论模式 (discuss): 学术讨论，不修改内容
- 撰写模式 (write): 直接修改论文内容

// 支持的 AI 模型
- DeepSeek Chat
- DeepSeek Reasoner (深度推理)
- Qwen (通义千问)

// 工具调用
- 自动解析 AI 响应中的工具调用
- 最多执行 10 次工具迭代
- 流式响应支持
```

### 4. PreviewPanel.tsx - 预览/工具面板
**职责**：工具栏、预览和资源管理入口

```typescript
// 功能模块
- 工具栏（可折叠）
  - 配置 API Key
  - 保存/打开文件
  - 导出聊天记录
  - 查看工具历史
- 预览面板
- 资源管理面板
```

### 5. ResourcePanel.tsx - 资源管理面板
**职责**：管理所有类型的学术资源

```typescript
// 资源类型
- references: 参考文献
- images: 图片
- pdfs: PDF 文献
- datafiles: 数据文件
- codesnippets: 代码片段
- notes: 笔记

// 功能
- CRUD 操作
- 文件上传
- 引用生成 (LaTeX/Markdown)
```

### 6. ApiKeyModal.tsx - API Key 配置弹窗
**职责**：配置和管理 AI 服务的 API Key

```typescript
// 支持的服务
- DeepSeek API
- Qwen API (通义千问)

// 功能
- API Key 验证
- 连接测试
- 安全存储 (Base64 编码)
```

### 7. ErrorBoundary.tsx - 错误边界
**职责**：捕获组件渲染错误，防止应用崩溃

---

## 🔧 后端模块详解

### 1. config.cjs - 配置模块
```javascript
// 导出配置
- PORT: 3001                    // 服务器端口
- STORAGE_DIR: './storage'      // 存储目录
- RESOURCE_TYPES: [...]         // 资源类型列表
- MODEL_CONFIG: {...}           // AI 模型配置
```

### 2. storage.cjs - 存储模块
```javascript
// 功能
- 文件系统持久化
- localStorage 兼容接口
- 内存缓存加速

// 导出
- localStorage: object          // 存储接口
- initializeStorage(): void     // 初始化
```

### 3. pdfUtils.cjs - PDF 工具模块
```javascript
// 功能
- PDF.js 动态加载
- PDF 文本提取
- 元数据解析

// 导出
- extractPDFText(filePath): Promise<Result>
```

### 4. aiApi.cjs - AI API 模块
```javascript
// 导出函数
- callDeepSeekApi(prompt, apiKey, model)      // 普通调用
- callDeepSeekApiStream(prompt, apiKey, model, onChunk)  // 流式调用
- callQwenApi(prompt, apiKey)                 // Qwen 调用
- generatePrompt(content, input, mode, tools, messages)  // 生成提示词
```

### 5. tools.cjs - 工具模块
```javascript
// 可用工具 (AVAILABLE_TOOLS)
1. search_papers    - arXiv 论文搜索
2. download_paper   - 下载论文 PDF
3. read_pdf_content - 读取 PDF 内容
4. view_file        - 查看编辑器内容
5. edit_file        - 修改编辑器内容
6. search_in_file   - 在文件中搜索
7. list_resources   - 列出资源
8. add_resource     - 添加资源
9. insert_resource  - 插入资源引用

// 导出
- AVAILABLE_TOOLS: Tool[]
- executeTool(name, params, content): Promise<Result>
- parseToolCalls(response): ToolCall[]
- generateInsertContent(resource, type, format): string
```

---

## 🌐 API 接口文档

### Agent API
| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/agent` | AI 对话（普通响应） |
| POST | `/api/agent/stream` | AI 对话（流式响应） |

### Tools API
| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/tools` | 获取可用工具列表 |
| POST | `/api/tools/execute` | 执行工具调用 |

### Resources API
| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/resources/:type` | 获取资源列表 |
| POST | `/api/resources/:type` | 添加资源 |
| PUT | `/api/resources/:type/:id` | 更新资源 |
| DELETE | `/api/resources/:type/:id` | 删除资源 |
| POST | `/api/resources/:type/:id/insert` | 生成资源引用 |

### Health API
| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/health` | 健康检查 |

---

## 🛠️ 技术栈

### 前端
| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.2.0 | UI 框架 |
| TypeScript | 5.9.3 | 类型系统 |
| Vite | 5.0.8 | 构建工具 |
| Monaco Editor | 4.6.0 | 代码编辑器 |
| React Markdown | 10.1.0 | Markdown 渲染 |
| Axios | 1.13.2 | HTTP 客户端 |

### 后端
| 技术 | 版本 | 用途 |
|------|------|------|
| Express | 4.18.2 | Web 框架 |
| pdfjs-dist | 4.0.379 | PDF 解析 |
| xml2js | 0.6.2 | XML 解析 (arXiv) |
| fs-extra | 11.3.3 | 文件操作 |
| dotenv | 17.2.3 | 环境变量 |

---

## 🚀 启动指南

### 开发模式
```bash
# 1. 安装依赖
npm install

# 2. 启动后端服务器
node server/index.cjs

# 3. 启动前端开发服务器 (新终端)
npm run dev
```

### 生产构建
```bash
# 构建前端
npm run build

# 启动服务器
node server/index.cjs
```

---

## 📊 数据流图

```
用户输入 ──▶ AgentChat ──▶ agentService ──▶ 后端 /api/agent
                                              │
                                              ▼
                                         AI API 调用
                                        (DeepSeek/Qwen)
                                              │
                                              ▼
                                         解析工具调用
                                              │
                                              ▼
                                         executeTool
                                              │
                     ┌────────────────────────┼────────────────────────┐
                     │                        │                        │
                     ▼                        ▼                        ▼
               arXiv 搜索              PDF 处理               资源管理
                     │                        │                        │
                     └────────────────────────┼────────────────────────┘
                                              │
                                              ▼
                                         返回结果
                                              │
                                              ▼
                                     更新 UI / 编辑器
```

---

## 📝 注意事项

1. **API Key 安全**：API Key 使用 Base64 编码存储在 localStorage 中
2. **PDF 大小限制**：PDF 文件不能超过 100MB
3. **工具迭代限制**：单次对话最多执行 10 次工具调用
4. **自动保存**：编辑器内容每 2 秒自动保存
5. **存储持久化**：资源数据同时存储在内存和文件系统中

---

*文档生成时间: 2026-01-13*
